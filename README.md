# VAULT

A minimal black-on-black music library web app.

## Run locally
Just open `index.html` in your browser.

If your browser blocks local audio loading, run a tiny server:
- Python:
  - `python -m http.server 8080`
  - open http://localhost:8080
